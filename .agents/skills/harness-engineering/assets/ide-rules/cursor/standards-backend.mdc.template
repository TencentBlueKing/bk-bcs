---
description: 改后端前按节加载选用的后端规范
alwaysApply: false
globs: "**/*.{go,py,java,rs}"
---

# 后端规范加载

编辑匹配后端源码前：
1. 查 `docs/standards/README.md`「加载预算」与「章节快速索引」。
2. **按节 Read** `${STANDARD_PATH}` 中与本任务相关的章节；禁止默认全文灌入。

本 rule 不含完整规范正文。
