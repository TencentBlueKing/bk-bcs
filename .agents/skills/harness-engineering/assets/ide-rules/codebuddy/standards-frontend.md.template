---
enabled: true
alwaysApply: false
paths:
  - "**/*.{vue,ts,tsx,js,jsx,css,scss,less,sass}"
---

# 前端规范加载

编辑匹配前端源码前：
1. 查 `docs/standards/README.md`「加载预算」与「章节快速索引」。
2. **按节 Read** `${STANDARD_PATH}` 中与本任务相关的章节（组件/状态/路由/请求等）；禁止默认全文灌入。

本 rule 不含完整规范正文。
