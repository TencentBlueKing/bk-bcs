---
enabled: true
alwaysApply: true
---

# Standards 加载门闩

写或改业务代码前：
1. Read `docs/standards/README.md` 的「当前项目选用的规范」与「加载预算」。
2. 按预算与「章节快速索引」**只 Read 相关章节**（禁止无差别灌入整份长规范）。
3. 禁止把完整 standards 正文写入 Always 规则；完整条款见 `@docs/standards/README.md`。
4. 完成前按相关节清单自检，并运行仓库已有 lint/test/build（无脚本不得声称已通过）。
