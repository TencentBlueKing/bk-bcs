---
paths:
  - "**/*.{proto,yaml,yml}"
  - "**/swagger*"
  - "**/openapi*"
---

# 接口规范加载

编辑匹配接口相关文件前：
1. 查 `docs/standards/README.md`「加载预算」与索引。
2. **按节 Read** `${STANDARD_PATH}` 相关章节；鉴权/敏感接口另按预算读安全红线对应节。禁止默认全文灌入。

本 rule 不含完整规范正文。
