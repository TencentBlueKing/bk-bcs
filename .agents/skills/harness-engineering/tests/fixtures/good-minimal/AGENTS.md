# Demo Project

Agent 认知入口：短地图 + 指向 docs/harness 与 docs/standards。

## 编码前必读（门闩）

写或改业务代码前：
1. 打开 `docs/standards/README.md`，确认「当前项目选用的规范」与「加载预算」。
2. 按预算与索引**只 Read 相关章节**（禁止无差别灌入整份长规范）。
3. 未选用的端不得假装存在规范。
4. 完成前按相关节清单自检并运行仓库已有 lint/test/build（无脚本不得声称已通过）。
