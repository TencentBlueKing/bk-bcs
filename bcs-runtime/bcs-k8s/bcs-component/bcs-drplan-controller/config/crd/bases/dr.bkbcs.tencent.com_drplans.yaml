---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: drplans.dr.bkbcs.tencent.com
spec:
  group: dr.bkbcs.tencent.com
  names:
    kind: DRPlan
    listKind: DRPlanList
    plural: drplans
    singular: drplan
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DRPlan is the Schema for the drplans API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of DRPlan
            properties:
              description:
                description: Description is the plan description
                type: string
              failurePolicy:
                default: Stop
                description: FailurePolicy defines how to handle failures
                enum:
                - Stop
                - Continue
                type: string
              globalParams:
                description: GlobalParams are global parameters passed to all workflows
                items:
                  description: Parameter defines a parameter with name, type, and
                    value/default
                  properties:
                    default:
                      description: Default is the default value (used when Value is
                        not set, e.g. in workflow/plan parameter definitions)
                      type: string
                    description:
                      description: Description is the parameter description
                      type: string
                    name:
                      description: Name is the parameter name
                      type: string
                    required:
                      description: Required indicates if the parameter is required
                      type: boolean
                    type:
                      default: string
                      description: 'Type is the parameter type: string (default),
                        number, boolean'
                      enum:
                      - string
                      - number
                      - boolean
                      type: string
                    value:
                      description: Value is the explicit value to use (e.g. when passing
                        params in DRPlan stage). Takes precedence over Default when
                        both set.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              stages:
                description: Stages are the stage orchestration list
                items:
                  description: Stage defines a stage in the plan orchestration
                  properties:
                    dependsOn:
                      description: DependsOn lists stage names this stage depends
                        on
                      items:
                        type: string
                      type: array
                    description:
                      description: Description is the stage description
                      type: string
                    failurePolicy:
                      description: FailurePolicy is the stage-level failure policy
                        (overrides global)
                      enum:
                      - Stop
                      - Continue
                      - FailFast
                      type: string
                    name:
                      description: Name is the unique stage name
                      minLength: 1
                      type: string
                    parallel:
                      default: false
                      description: Parallel indicates whether to execute all workflows
                        in this stage in parallel
                      type: boolean
                    workflows:
                      description: Workflows are the workflow reference list
                      items:
                        description: WorkflowReference defines a workflow reference
                          with parameters
                        properties:
                          params:
                            description: Params are parameters that override globalParams
                            items:
                              description: Parameter defines a parameter with name,
                                type, and value/default
                              properties:
                                default:
                                  description: Default is the default value (used
                                    when Value is not set, e.g. in workflow/plan parameter
                                    definitions)
                                  type: string
                                description:
                                  description: Description is the parameter description
                                  type: string
                                name:
                                  description: Name is the parameter name
                                  type: string
                                required:
                                  description: Required indicates if the parameter
                                    is required
                                  type: boolean
                                type:
                                  default: string
                                  description: 'Type is the parameter type: string
                                    (default), number, boolean'
                                  enum:
                                  - string
                                  - number
                                  - boolean
                                  type: string
                                value:
                                  description: Value is the explicit value to use
                                    (e.g. when passing params in DRPlan stage). Takes
                                    precedence over Default when both set.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                          workflowRef:
                            description: WorkflowRef is the workflow reference (name
                              + namespace)
                            properties:
                              name:
                                description: Name is the object name
                                type: string
                              namespace:
                                description: Namespace is the object namespace
                                type: string
                            required:
                            - name
                            type: object
                        required:
                        - workflowRef
                        type: object
                      minItems: 1
                      type: array
                  required:
                  - name
                  - workflows
                  type: object
                minItems: 1
                type: array
            required:
            - stages
            type: object
          status:
            description: status defines the observed state of DRPlan
            properties:
              conditions:
                description: Conditions represent the current state of the DRPlan
                  resource
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentExecution:
                description: CurrentExecution is the currently running execution reference
                  (for concurrency control)
                properties:
                  name:
                    description: Name is the object name
                    type: string
                  namespace:
                    description: Namespace is the object namespace
                    type: string
                required:
                - name
                type: object
              executionHistory:
                description: ExecutionHistory keeps a list of recent execution references
                  (max 10, newest first)
                items:
                  description: ExecutionRecord records a historical execution reference
                  properties:
                    completionTime:
                      description: CompletionTime is the execution completion time
                      format: date-time
                      type: string
                    name:
                      description: Name is the DRPlanExecution name
                      type: string
                    namespace:
                      description: Namespace is the DRPlanExecution namespace
                      type: string
                    operationType:
                      description: 'OperationType is the operation type: Execute,
                        Revert'
                      type: string
                    phase:
                      description: 'Phase is the execution phase: Pending, Running,
                        Succeeded, Failed'
                      type: string
                    startTime:
                      description: StartTime is the execution start time
                      format: date-time
                      type: string
                  required:
                  - name
                  - namespace
                  - operationType
                  type: object
                maxItems: 10
                type: array
              lastExecutionRef:
                description: LastExecutionRef is the last execution record name
                type: string
              lastExecutionTime:
                description: LastExecutionTime is the last execution time
                format: date-time
                type: string
              lastProcessedTrigger:
                description: |-
                  LastProcessedTrigger is DEPRECATED and no longer used (annotation trigger removed).
                  Kept for backward compatibility, will be removed in future versions.
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation observed by
                  the controller
                format: int64
                type: integer
              phase:
                description: 'Phase is the plan phase: Ready, Executed, Invalid'
                enum:
                - Ready
                - Executed
                - Invalid
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
