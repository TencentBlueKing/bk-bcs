{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "bcs-drplan-controller.fullname" . }}-manager-rolebinding
  labels:
    {{- include "bcs-drplan-controller.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "bcs-drplan-controller.fullname" . }}-manager-role
subjects:
- kind: ServiceAccount
  name: {{ include "bcs-drplan-controller.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}
