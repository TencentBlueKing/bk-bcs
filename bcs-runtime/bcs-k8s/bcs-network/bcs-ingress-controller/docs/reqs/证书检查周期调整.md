# [bcs-ingress-controller] 证书过期检查周期调整

## 基本信息

| 字段 | 值 |
|------|-----|
| 需求 ID | 1070046748135050873 |
| 需求名称 | [bcs-ingress-controller] 新增证书过期时间指标（增量修订：检查周期） |
| 优先级 | Middle |
| 处理人 | adelaidahe |
| 父需求 | 无（本修订作用于父需求 #1070046748135050873） |
| 创建时间 | 2026-06-11 |
| 原始需求文档 | docs/reqs/证书过期指标.md |
| 本地修订文档 | docs/reqs/证书检查周期调整.md |

## 需求背景

### 业务背景

父需求 #1070046748135050873 已实现 `CertificateChecker`，以 **每 10 分钟** 执行一次全量证书过期巡检，并上报 Prometheus 指标 `bkbcs_ingressctrl_certificate_days_until_expiry` 等。

在实际运行中，10 分钟周期对 SSL 证书过期监控而言过于频繁：证书有效期通常以月/年计，10 分钟粒度带来的 API 调用与 Controller 负载收益有限。将巡检周期调整为 **每 1 小时（60 分钟）** 可在保持过期风险可感知的前提下，降低腾讯云 SSL API 调用频率与 Controller 资源消耗。

**目标用户**：BCS 集群运维人员、平台 SRE。

**不做本修订的影响**：维持 10 分钟周期，SSL API 调用量约为 1 小时周期的 6 倍，对大规模集群产生不必要的云 API 压力。

### 用户故事

作为 **平台 SRE**  
我想要 **证书过期检查以 1 小时为周期执行**  
以便于 **降低 SSL API 调用频率与 Controller 巡检负载，同时仍能在证书过期前通过 Prometheus 指标触发告警**

### 需求来源

- **需求渠道**：技术优化
- **关联需求**：父需求 #1070046748135050873
- **参考资料**：`internal/check/checkrunner.go`、`main.go` 中 `CertificateChecker` 注册逻辑

## 功能需求

### 核心功能点

| 功能编号 | 功能描述 | 优先级 | 涉及角色 | 备注 |
|---------|---------|--------|---------|------|
| F-011 | 将 CertificateChecker 执行周期从 10 分钟调整为 60 分钟 | P0 | SRE | 必须；仅影响证书过期检查 |

### 详细功能描述

#### [F-011] 证书过期检查周期调整

- **输入**：Controller 启动且满足 CertificateChecker 注册条件（腾讯云 + `--certificate_check_enabled=true`）
- **处理逻辑**：
  1. CertificateChecker 的定时执行周期由 **每 10 分钟** 改为 **每 60 分钟（1 小时）**
  2. 仅调整 CertificateChecker 的调度周期；其它 Checker（PortBindChecker、ListenerChecker 等，1 分钟周期）**不受影响**
  3. 检查周期为固定值 60 分钟，**不新增** CLI / Helm 可配置项（与当前 10 分钟为固定值的行为一致）
  4. CertificateChecker 的单轮 Check 逻辑（List Ingress、展开 Binding、DescribeCertificate 查询、指标上报与清理）**不变**
- **输出**：Prometheus 证书过期指标每 60 分钟刷新一次
- **边界条件**：
  - Controller 重启后，首轮 Check 在注册后最多 60 分钟内触发（与现有 CheckRunner ticker 行为一致）
  - 指标数据最大 staleness 由 10 分钟变为 60 分钟
- **异常处理**：与父需求一致，Check 失败不影响 Controller 其它功能

## 非功能需求

### 性能需求

- **检查周期**：开启后每 **60 分钟** 执行一次全量 Check（原 10 分钟）
- **API 调用量**：每个唯一 CertificateId 每 **60 分钟** 产生 1 次 `DescribeCertificate` 请求（原每 10 分钟 1 次，调用频率降低约 6 倍）
- **并发能力**：与父需求一致，Checker 在独立 goroutine 执行，不阻塞 Reconcile 主流程

### 可用性与稳定性

- **指标 staleness**：证书过期天数指标最大延迟由 10 分钟增至 60 分钟；运维告警策略须接受此粒度（证书有效期以月/年计，60 分钟延迟可接受）
- **向后兼容**：升级后仅周期变化，指标名称、label、开关逻辑不变

## 业务规则

### 业务逻辑规则

- **规则 R-007**：CertificateChecker 执行周期固定为 60 分钟，不可通过 CLI / Helm 配置
- **规则 R-008**：其它周期性 Checker（1 分钟周期）不受本修订影响

## 验收标准

### 功能验收

- [ ] **AC-018**：Given 腾讯云环境且 `--certificate_check_enabled=true` When Controller 启动 Then CertificateChecker 注册成功且每 **60 分钟** 触发一次 `Run()`（非 10 分钟）
- [ ] **AC-019**：Given CertificateChecker 已注册 When 观察 2 小时内 Check 执行次数 Then 执行次数为 2 次（±启动时刻偏差）
- [ ] **AC-020**：Given `--certificate_check_enabled=true` 且集群中存在 HTTPS Ingress、certID 有效、SSL API 权限正常 When CertificateChecker 执行一轮 Check Then 指标行为与父需求 AC-001 一致（`days_until_expiry` + `query_success=1`），仅刷新频率变为 60 分钟
- [ ] **AC-021**：Given Controller 运行中 When 观察 PortBindChecker / ListenerChecker 等其它 Checker Then 仍保持 **1 分钟** 周期，不受本修订影响

### 性能验收

- [ ] **AC-P03**：Given `--certificate_check_enabled=true` 且集群 500 个 Ingress、200 个唯一 certID When Check 执行 Then 单次 Check 在 **60 分钟** 周期内完成（含限流等待；原 AC-P01 中「10 分钟内完成」随周期同步修订为 60 分钟）

## 边界范围

### 本期包含

- CertificateChecker 调度周期 10 分钟 → 60 分钟
- 需求文档、验收标准中涉及「10 分钟周期」的描述同步修订为「60 分钟」

### 本期不包含

- 新增 CLI / Helm 参数使检查周期可配置
- 调整其它 Checker（1 分钟周期）的执行频率
- 修改 CertificateChecker 单轮 Check 的业务逻辑、指标定义或 SSL API 调用方式

## 约束条件

- **架构约束**：复用现有 `CheckRunner` 调度机制；仅调整 CertificateChecker 注册的 interval 档位
- **兼容性**：升级后已部署集群在滚动重启后自动生效，无需额外配置

## 澄清记录

### 第 5 轮澄清（增量：检查周期调整）

**提问时间**：2026-06-11

**用户诉求（已确认，无需额外追问）**：

1. 将证书过期检查的执行周期从 **10 分钟** 修改为 **1 小时（60 分钟）**
2. 作为父需求 #1070046748135050873 的增量修订

**Agent 确认结论**：

| 项 | 决策 |
|----|------|
| 调整范围 | 仅 CertificateChecker |
| 新周期 | 固定 60 分钟 |
| 是否可配置 | 否（与当前 10 分钟固定行为一致） |
| 其它 Checker | 不受影响（仍为 1 分钟） |
| 指标逻辑 | 不变，仅刷新频率降低 |

**标注**：用户在本轮澄清中已明确变更意图，需求描述充分，无需额外交互式澄清。

---

## 原需求描述

> 针对需求 #1070046748135050873，将证书过期检查的时间从 10 分钟执行一次修改为 1 小时（60 分钟）执行一次。

## 对父需求文档的修订摘要

以下章节须同步更新（实现阶段一并修订 `docs/reqs/证书过期指标.md` 及 TAPD 父需求描述）：

| 原描述 | 修订后 |
|--------|--------|
| 执行周期：每 10 分钟（`CheckPer10Min`） | 执行周期：每 60 分钟（1 小时） |
| FR-006：周期 CheckPer10Min，10 分钟 | FR-006：周期 60 分钟 |
| AC-011：每 10 分钟执行 | AC-011：每 60 分钟执行 |
| AC-P01：单次 Check 在 10 分钟内完成 | AC-P01：单次 Check 在 60 分钟周期内完成 |
| AC-T01：每 10 分钟触发 Run() | AC-T01：每 60 分钟触发 Run() |
| F-009 输出：每 10 分钟全量轮询 | F-009 输出：每 60 分钟全量轮询 |
| 非功能-检查周期：每 10 分钟 | 非功能-检查周期：每 60 分钟 |
