# .env.production 生产模式生效

# 登录地址 (关联: 退出登录)
BK_LOGIN_FULL = '{{ .LOGIN_FULL }}'

# 蓝盾地址 (关联: 跳转蓝盾项目管理)
BK_DEVOPS_HOST = '{{ .DEVOPS_HOST }}'

# python API地址
BK_DEVOPS_BCS_API_URL = '{{ .DEVOPS_BCS_API_URL }}'

# 静态资源地址
BK_STATIC_URL = '{{ .STATIC_URL }}'

# 当前后端环境
BK_RUN_ENV = '{{ .RUN_ENV }}'

# 镜像地址
BK_DEVOPS_ARTIFACTORY_HOST = '{{ .DEVOPS_ARTIFACTORY_HOST }}'

# 当前版本
BK_REGION = '{{ .REGION }}'

# 路由前缀
BK_SITE_URL = '{{ .SITE_URL }}'

# 权限中心地址
BK_IAM_HOST = '{{ .BK_IAM_HOST }}'

# PaaS平台地址
BK_PAAS_HOST = '{{ .PAAS_HOST }}'

# 监控地址
BK_BKMONITOR_HOST = '{{ .BKMONITOR_HOST }}'
 
# 下沉服务地址
BK_BCS_API_HOST = '{{ .BCS_API_HOST }}'

# 允许加载的域名
BK_PREFERRED_DOMAINS = '{{ .PREFERRED_DOMAINS }}'

BK_CC_HOST = '{{ .BK_CC_HOST }}'

# 前端文档配置

BK_BCS_CONFIG = '{{ .BCS_CONFIG }}'

# 服务器申请地址

BK_SRE_HOST = '{{ .BK_SRE_HOST }}'

BK_BCS_NAMESPACE_PREFIX = '{{ .BCS_NAMESPACE_PREFIX }}'
