apiVersion: v1
kind: Service
metadata:
  name: bcs-public-cluster-webhook
  namespace: {{ .Release.Namespace }}
  labels:
    app: bcs-public-cluster-webhook
spec:
  ports:
    - port: 443
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: bcs-public-cluster-webhook
