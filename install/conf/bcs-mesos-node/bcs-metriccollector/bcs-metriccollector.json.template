{
  "v": ${log_level},
  "alsologtostderr": true,
  "log_dir": "${log_dir}",
  "pid_dir": "${pid_dir}",
  "clusterid": "${clusterId}",
  "clustertype": "${clusterType}",
  "namespace": "${collectorNamespace}",
  "name": "${collectorName}",
  "auth_token": "${metricToken}",
  "exporter_address": "${exporterServer}",
  "metric_server_address": "${metricServer}",
  "ca_file": "${caFile}",
  "client_cert_file": "${clientCertFile}",
  "client_key_file": "${clientKeyFile}"
}